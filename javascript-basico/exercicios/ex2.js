const nome = "Rafael";
const idade = 30;
const peso = 84;
const altura = 1.8;
const imc = peso / (altura * altura);

console.log(
  `${nome} tem ${idade} anos, pesa ${peso} kg teem ${altura} de altura e seu imc é de ${imc}`
);
